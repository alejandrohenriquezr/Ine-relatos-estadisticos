import {NextResponse} from "next/server";
import {parseVitalStatistics} from "../../../lib/vital-statistics-data";
import {latestPublishedAnalysisFiles} from "../../../lib/published-analysis-files";
export async function GET(){const db=(globalThis as typeof globalThis&{__SITES_DB?:D1Database}).__SITES_DB;if(!db)return NextResponse.json({error:"La caché compartida aún no está disponible"},{status:503});await db.prepare("CREATE TABLE IF NOT EXISTS economic_source_cache (kind TEXT PRIMARY KEY, source_url TEXT NOT NULL, source_last_modified TEXT, source_etag TEXT, source_size TEXT, payload_json TEXT NOT NULL, checked_at TEXT NOT NULL, updated_at TEXT NOT NULL)").run();const cached=await db.prepare("SELECT * FROM economic_source_cache WHERE kind='vital-statistics'").first<Record<string,string>>();try{const source=await latestPublishedAnalysisFiles("nacimientos",{workbook:/vital|nacimiento|defunc|matrimonio|[.]xlsx/i}),signature=source.signature,now=new Date().toISOString();if(cached?.source_last_modified===signature){await db.prepare("UPDATE economic_source_cache SET checked_at=? WHERE kind='vital-statistics'").bind(now).run();return NextResponse.json({...JSON.parse(cached.payload_json),source:source.metadata.workbook,cache:{status:"shared",checkedAt:now}},{headers:{"Cache-Control":"no-store","X-Analysis-Source":"internal-published"}});}const files=await source.readAll(),payload=parseVitalStatistics(files.workbook);await db.prepare("INSERT INTO economic_source_cache(kind,source_url,source_last_modified,source_etag,source_size,payload_json,checked_at,updated_at) VALUES(?,?,?,?,?,?,?,?) ON CONFLICT(kind) DO UPDATE SET source_url=excluded.source_url,source_last_modified=excluded.source_last_modified,payload_json=excluded.payload_json,checked_at=excluded.checked_at,updated_at=excluded.updated_at").bind("vital-statistics",JSON.stringify(source.metadata),signature,null,String(source.metadata.workbook.size),JSON.stringify(payload),now,now).run();return NextResponse.json({...payload,source:source.metadata.workbook,cache:{status:"updated",checkedAt:now}},{headers:{"Cache-Control":"no-store","X-Analysis-Source":"internal-published"}});}catch(error){if(cached)return NextResponse.json({...JSON.parse(cached.payload_json),source:null,cache:{status:"stale",checkedAt:cached.checked_at,updatedAt:cached.updated_at}},{headers:{"Cache-Control":"no-store","X-Analysis-Source":"shared-cache","X-Data-Warning":"stale"}});return NextResponse.json({error:error instanceof Error?error.message:"Error de datos vitales"},{status:503});}}
